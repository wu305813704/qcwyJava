<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcwy.dao.WxOrderMessageDao">
    <insert id="save" useGeneratedKeys="true" keyProperty="msg.msg_id">
        insert into t_wx_order_msg (openid, order_no, type,send_time)
        values (#{msg.openid},#{msg.order_no},#{msg.type},now())
    </insert>

    <select id="getMsgByJobNo" resultType="com.qcwy.entity.WxOrderMessage">
        select * from t_wx_order_msg
        where openid = #{openid}
        order by send_time desc
    </select>

</mapper>