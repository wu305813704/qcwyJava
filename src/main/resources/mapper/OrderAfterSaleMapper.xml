<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qcwy.dao.OrderAfterSaleDao">
    <insert id="save">
        INSERT INTO t_xt_order_after_sale
        (order_no,cause,time)
        VALUES
        (#{orderNo},#{cause},now())
    </insert>
</mapper>